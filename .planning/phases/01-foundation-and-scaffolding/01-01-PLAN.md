---
phase: 01-foundation-and-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .env.example
  - .env.local
  - CLAUDE.md
  - docker-compose.yml
  - prisma.config.ts
  - next.config.ts
  - postcss.config.mjs
  - tsconfig.json
  - package.json
  - pnpm-lock.yaml
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/page.tsx
  - src/lib/utils.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "Running `pnpm dev` starts the development server at localhost:3000"
    - "Running `docker compose up -d` starts PostgreSQL accessible at localhost:5432"
    - "`.gitignore` exists and blocks `.env.local`, `node_modules/`, `.next/`, `src/generated/prisma`"
    - "`.env.example` lists all required env vars with placeholder values"
    - "`CLAUDE.md` documents project stack, conventions, and verification command"
    - "`git status` after setup shows no sensitive files"
  artifacts:
    - path: ".gitignore"
      provides: "Security filter for public repo"
      contains: ".env"
    - path: "docker-compose.yml"
      provides: "PostgreSQL dev container"
      contains: "postgres:17-alpine"
    - path: "prisma.config.ts"
      provides: "Prisma 7 CLI config with dotenv"
      contains: "defineConfig"
    - path: "package.json"
      provides: "Project manifest with all Phase 1 dependencies"
      contains: "@clerk/nextjs"
    - path: ".env.example"
      provides: "Env var template for contributors"
      contains: "DATABASE_URL"
    - path: "CLAUDE.md"
      provides: "Project documentation for Claude"
      contains: "type-check"
    - path: "src/app/layout.tsx"
      provides: "Root layout with ClerkProvider and fonts"
      contains: "ClerkProvider"
    - path: "src/lib/utils.ts"
      provides: "cn() utility and slugify()"
      contains: "twMerge"
  key_links:
    - from: "prisma.config.ts"
      to: ".env.local"
      via: "dotenv loading"
      pattern: 'dotenv\.config.*\.env\.local'
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: 'import.*globals\.css'
---

<objective>
Scaffold the PrimorHolding Next.js 16 project from scratch with all dependencies, Docker PostgreSQL, configuration files, and project documentation.

Purpose: Establish the secure, runnable project foundation that all subsequent plans depend on. The .gitignore MUST be the first file created since this is a public GitHub repo.
Output: A runnable Next.js 16 project with Docker PostgreSQL, all deps installed, env templates, and CLAUDE.md.
</objective>

<execution_context>
@/home/sth/.claude/get-shit-done/workflows/execute-plan.md
@/home/sth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-scaffolding/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .gitignore and scaffold Next.js 16 project</name>
  <files>
    .gitignore,
    package.json,
    pnpm-lock.yaml,
    tsconfig.json,
    next.config.ts,
    postcss.config.mjs,
    docker-compose.yml,
    prisma.config.ts,
    src/app/layout.tsx,
    src/app/globals.css,
    src/app/page.tsx,
    src/lib/utils.ts
  </files>
  <action>
**CRITICAL: .gitignore must be the first file written -- before any other file, before `pnpm create`, before anything. This is a public GitHub repo.**

Step 1 -- Create `.gitignore` at project root with this exact content (copy from research 01-RESEARCH.md "gitignore" section):
- node_modules, .pnp, .yarn
- .next/, /out/, /build, /coverage
- .DS_Store, *.pem, debug logs
- `.env*` with `!.env.example` exception
- .vercel, *.tsbuildinfo, next-env.d.ts
- `/src/generated/prisma` (Prisma 7 generated client)

Step 2 -- Run `pnpm create next-app@16.1.6 . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --turbopack` in `/home/sth/PrimorHolding/`. The `.gitignore` already exists, so the scaffolder will NOT overwrite it (pnpm create respects existing .gitignore). If it prompts for project name, use "primor-holding".

Step 3 -- Install production dependencies:
```bash
pnpm add @prisma/adapter-neon@^7.4.2 @neondatabase/serverless@^1.0.2 dotenv@^17.3.1
pnpm add @clerk/nextjs@^7.0.4
pnpm add class-variance-authority@^0.7.1 clsx@^2.1.1 tailwind-merge@^3.5.0 lucide-react@^0.577.0
```

Step 4 -- Install dev dependencies:
```bash
pnpm add -D prisma@^7.4.2 tsx@^4.21.0 sharp@^0.34 @types/node@^20 @types/react@^19 @types/react-dom@^19
```

Step 5 -- Run `pnpm approve-builds` if prompted (select Prisma + esbuild). This is required for Prisma 7 post-install.

Step 6 -- Update `package.json` scripts to include:
```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "type-check": "tsc --noEmit",
    "db:push": "prisma db push",
    "db:seed": "tsx prisma/seed.ts",
    "db:studio": "prisma studio",
    "postinstall": "prisma generate"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
```

Step 7 -- Create `docker-compose.yml` at project root. PostgreSQL 17-alpine, port 5432, user `primor`, password `primor_dev`, database `primor_holding`, named volume `pgdata`. Exact content in 01-RESEARCH.md Pattern 5.

Step 8 -- Create `prisma.config.ts` at project root. Uses `dotenv` to load `.env.local` then `.env`, defines config with `schema: "prisma/schema.prisma"`, `migrations.path: "prisma/migrations"`, datasource URL from `DIRECT_URL` or `DATABASE_URL`. Exact content in 01-RESEARCH.md Pattern 2.

Step 9 -- Create `next.config.ts`: `poweredByHeader: false`, images remotePatterns for `res.cloudinary.com`. Exact content in 01-RESEARCH.md.

Step 10 -- Create `postcss.config.mjs` with `@tailwindcss/postcss` plugin only (no other plugins). Exact content in 01-RESEARCH.md.

Step 11 -- Replace `src/app/globals.css` with the Tailwind v4 CSS-first theme from 01-RESEARCH.md Pattern 3. Includes `@import "tailwindcss"`, `@font-face` for Playfair Display, `:root` CSS variables (navy-dark, navy, navy-light, gold, gold-light, cream, white, slate, border-gold), `@theme inline` block mapping to Tailwind tokens, body styles, `.font-serif`, `.text-gradient-gold`, `.gold-glow`, `::selection`.

Step 12 -- Replace `src/app/layout.tsx` with root layout from 01-RESEARCH.md Pattern 6: imports Geist font from next/font/google, wraps children in ClerkProvider, sets `lang="pt-BR"`, includes metadata with title template "Primor Holding - Consultoria Empresarial" and pt_BR OpenGraph locale.

Step 13 -- Replace `src/app/page.tsx` with a placeholder landing page that demonstrates the theme is working: dark background, gold heading "Primor Holding" in serif font, cream subtitle text, and a gold button. This serves as a visual smoke test for FOUND-06 in the next plan.

Step 14 -- Create `src/lib/utils.ts` with `cn()` (clsx + tailwind-merge) and `slugify()` functions. Exact content in 01-RESEARCH.md.

Step 15 -- Create empty `prisma/schema.prisma` with just the generator and datasource blocks (models come in Plan 02):
```prisma
generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}
```

Step 16 -- Run `pnpm prisma generate` to create the generated client directory.

Step 17 -- Verify `.gitignore` is still intact and contains all required entries (the scaffolder may have modified it). If entries are missing, re-add them.
  </action>
  <verify>
    <automated>source /home/sth/.nvm/nvm.sh && cd /home/sth/PrimorHolding && pnpm type-check</automated>
  </verify>
  <done>
    - `.gitignore` blocks .env files, node_modules, .next, src/generated/prisma
    - `pnpm type-check` passes with zero errors
    - `docker-compose.yml` exists with PostgreSQL 17-alpine config
    - `prisma.config.ts` exists with dotenv loading
    - `next.config.ts` exists with poweredByHeader: false
    - `postcss.config.mjs` exists with @tailwindcss/postcss
    - `src/app/globals.css` has Tailwind v4 theme with navy/gold palette
    - `src/app/layout.tsx` has ClerkProvider, Geist font, pt-BR lang
    - `src/lib/utils.ts` exports cn() and slugify()
  </done>
</task>

<task type="auto">
  <name>Task 2: Create env template, .env.local, and CLAUDE.md</name>
  <files>
    .env.example,
    .env.local,
    CLAUDE.md
  </files>
  <action>
Step 1 -- Create `.env.example` at project root with ALL required env vars using placeholder values (no real secrets). Exact content from 01-RESEARCH.md ".env.example" section:
- `DATABASE_URL` and `DIRECT_URL` with Docker PostgreSQL defaults (`postgresql://primor:primor_dev@localhost:5432/primor_holding`)
- Clerk keys: `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in`, `NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/admin`
- Cloudinary: `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`
- Email: `RESEND_API_KEY`, `CONTACT_EMAIL`
- Site: `NEXT_PUBLIC_SITE_URL=http://localhost:3000`

Step 2 -- Create `.env.local` by copying `.env.example` with Docker PostgreSQL URLs already filled in (these are local dev values, not secrets). Clerk and Cloudinary keys remain as placeholders until user configures them. The `.env.local` file is gitignored.

Step 3 -- Create `CLAUDE.md` at project root documenting:
- Project name: Primor Holding -- Institutional consulting website
- Stack: Next.js 16, TypeScript, Tailwind CSS v4, Prisma 7 + Neon, Clerk v7, Cloudinary, Docker (dev), Vercel (prod)
- Package manager: pnpm
- Node: via nvm (`source /home/sth/.nvm/nvm.sh`)
- Theme: Dark navy #0A1628, Gold #C9A227, Cream #FBF8F1, Playfair Display + Geist Sans
- Auth: Clerk v7 admin only, proxy.ts (not middleware.ts)
- Prisma: import from `@/generated/prisma/client`, generator = `prisma-client` (not prisma-client-js)
- Database: PostgreSQL -- Docker locally, Neon in production
- Verification command: `source /home/sth/.nvm/nvm.sh && cd /home/sth/PrimorHolding && pnpm type-check`
- Build command: `pnpm build`
- Dev: `docker compose up -d && pnpm dev`
- Key conventions: Server Components by default, Server Actions for CRUD (no API routes), prices calculated server-side
- Gotchas: proxy.ts not middleware.ts, prisma-client not prisma-client-js, Tailwind v4 CSS-first (no tailwind.config.js), PrismaNeon works with Docker PostgreSQL too
- CNPJ: 59.120.382/0001-30

Step 4 -- Verify `.env.local` does NOT appear in `git status` (gitignored) and `.env.example` DOES appear.
  </action>
  <verify>
    <automated>cd /home/sth/PrimorHolding && test -f .env.example && test -f .env.local && test -f CLAUDE.md && echo "ALL_FILES_EXIST"</automated>
  </verify>
  <done>
    - `.env.example` exists with all required env var placeholders
    - `.env.local` exists with Docker PostgreSQL defaults filled in
    - `CLAUDE.md` exists with complete project documentation
    - `.env.local` is gitignored (does not appear in `git status`)
    - `.env.example` is NOT gitignored (appears in `git status`)
  </done>
</task>

</tasks>

<verification>
1. `pnpm type-check` passes with zero errors
2. `test -f .gitignore && test -f .env.example && test -f .env.local && test -f CLAUDE.md && test -f docker-compose.yml && test -f prisma.config.ts` -- all files exist
3. `.gitignore` contains entries for `.env*`, `!.env.example`, `node_modules`, `.next`, `src/generated/prisma`
4. `package.json` has all scripts (dev, build, type-check, db:push, db:seed, postinstall)
5. `src/app/globals.css` contains `@theme inline` with navy/gold color tokens
</verification>

<success_criteria>
- The project scaffolding is complete: all config files, dependencies installed, Docker config ready
- Security is established: .gitignore blocks all secrets, .env.example provides safe template
- Documentation exists: CLAUDE.md captures full project context for Claude sessions
- `pnpm type-check` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-scaffolding/01-01-SUMMARY.md`
</output>
